% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ogrenci_performans_kumele.R
\name{ogrenci_performans_kumele}
\alias{ogrenci_performans_kumele}
\title{Öğrenci Performansını K-Means ile Kümele}
\usage{
ogrenci_performans_kumele(
  veri,
  degiskenler = NULL,
  k = 3L,
  standartlastir = TRUE,
  etiketler = NULL,
  nstart = 25L,
  seed = NULL
)
}
\arguments{
\item{veri}{\code{data.frame}. Öğrenci verisi.}

\item{degiskenler}{Karakter vektörü. Kümelemede kullanılacak
\strong{sayısal} sütun adları. \code{NULL} (varsayılan) verilirse \code{veri}
içindeki tüm sayısal sütunlar seçilir.}

\item{k}{Küme sayısı. Varsayılan 3.}

\item{standartlastir}{Mantıksal. \code{TRUE} (varsayılan) ise
kümelemeden önce \code{scale()} uygulanır.}

\item{etiketler}{Karakter vektörü. Uzunluğu \code{k}'ye eşit olmalıdır.
\code{NULL} verilirse \code{c("Düşük","Orta","Yüksek")} (k = 3 için) veya
\verb{Kume_1 ... Kume_k} kullanılır.}

\item{nstart}{\code{\link[stats:kmeans]{stats::kmeans()}}'e iletilir. Birden fazla rastgele
başlangıç için. Varsayılan 25.}

\item{seed}{Tekrarlanabilirlik için \code{set.seed()} tohumu.
\code{NULL} verilirse tohum ayarlanmaz.}
}
\value{
Aşağıdaki öğeleri içeren bir \code{list}:
\describe{
\item{\code{veri}}{Girdi \code{data.frame}'ine \code{kume} (\code{integer}) ve
\code{kume_etiketi} (\code{factor}) sütunları eklenmiş hali.}
\item{\code{kmeans}}{\code{\link[stats:kmeans]{stats::kmeans()}} çıktısı.}
\item{\code{merkezler}}{Orijinal ölçekte küme merkezleri
(\code{data.frame}).}
\item{\code{ozet}}{Küme bazlı gözlem sayısı ve her değişken için
ortalama içeren \code{data.frame}.}
\item{\code{argumanlar}}{Çağrıda kullanılan argümanların bir listesi.}
}
}
\description{
Bir öğrenci veri çerçevesindeki sayısal performans değişkenlerini
kullanarak k-means kümeleme yapar. Standartlaştırma, etiketleme ve
çoklu başlangıç adımlarını otomatik yönetir.
}
\details{
İçeride \code{\link[stats:kmeans]{stats::kmeans()}} çağrılır. Değişkenler varsayılan olarak
z-standartlaştırılır (\code{scale()}); bu, farklı ölçekteki puanların
kümelemede adil temsilini sağlar.

Küme etiketleri, kümelerin tüm kümeleme değişkenlerinin ortalaması
üzerinden sıralanmasıyla atanır: en düşük ortalamalı küme ilk
etikete, en yüksek ortalamalı küme son etikete gider. Böylece
\code{etiketler = c("Düşük","Orta","Yüksek")} verildiğinde atamalar
istatistiksel olarak anlamlıdır.
}
\examples{
data(ogrenci_notlari)

# En basit kullanim - tum sayisal sutunlarla k = 3
sonuc <- ogrenci_performans_kumele(
  veri = ogrenci_notlari,
  degiskenler = c("vize", "final"),
  seed = 42
)
head(sonuc$veri)
sonuc$merkezler

# Farkli k ve ozel etiketler
sonuc2 <- ogrenci_performans_kumele(
  veri        = ogrenci_notlari,
  degiskenler = c("vize", "final", "ortalama"),
  k           = 4,
  etiketler   = c("Risk", "Standart", "Iyi", "Ust"),
  seed        = 1
)
table(sonuc2$veri$kume_etiketi)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performans_sinifi_ata.R
\name{performans_sinifi_ata}
\alias{performans_sinifi_ata}
\title{Performans Sınıfı Ata (Kural veya Kantil Tabanlı)}
\usage{
performans_sinifi_ata(
  puan,
  yontem = c("kural", "kantil"),
  kesim_noktalari = c(50, 60, 70, 80, 90),
  etiketler = NULL,
  n_sinif = 4,
  na.rm = TRUE
)
}
\arguments{
\item{puan}{Sayısal vektör (\code{numeric}). Sınıflandırılacak puanlar.}

\item{yontem}{Sınıflandırma yöntemi. \code{"kural"} (varsayılan) veya
\code{"kantil"}.}

\item{kesim_noktalari}{Sayısal vektör. \code{yontem = "kural"} için
kullanılan iç kesim noktaları. Varsayılan \code{c(50, 60, 70, 80, 90)}
beş kesim noktası oluşturur; sonuçta 6 sınıf üretilir.}

\item{etiketler}{Karakter vektör. Her bir sınıfın etiketi.
Uzunluğu, oluşturulan sınıf sayısına eşit olmalıdır.
\code{yontem = "kural"} için varsayılan
\code{c("FF","DD","CC","BB","BA","AA")}; \code{yontem = "kantil"} için
\code{n_sinif} uzunluğunda otomatik üretilir.}

\item{n_sinif}{\code{yontem = "kantil"} için sınıf sayısı. Varsayılan 4
(çeyrekler).}

\item{na.rm}{Mantıksal. \code{TRUE} ise \code{puan} içindeki \code{NA}'lar
sınıflandırılmadan \code{NA} olarak döner.}
}
\value{
\code{puan} ile aynı uzunlukta, sıralı bir \code{factor} nesnesi.
Seviyeler \code{etiketler} argümanıyla aynı sırada olur.
}
\description{
Sayısal bir puan vektörünü, iki farklı yöntemden birini kullanarak
performans sınıflarına (faktör) atar.
}
\details{
\itemize{
\item \code{yontem = "kural"}: Kullanıcı tarafından verilen sabit kesim
noktalarına (\code{kesim_noktalari}) göre sınıflandırır. Geleneksel
harf notu sistemi için uygundur.
\item \code{yontem = "kantil"}: Verinin kendi dağılımına dayalı, eşit
yoğunluklu gruplara böler (örn. çeyrekler, beşli dilimler).
Göreceli performans değerlendirmesi için uygundur.
}
}
\examples{
# 1) Kural tabanlı harf notu ataması
notlar <- c(45, 62, 77, 88, 95, 51)
performans_sinifi_ata(notlar)

# 2) Kendi kesim noktalarınız
performans_sinifi_ata(
  notlar,
  kesim_noktalari = c(60, 80),
  etiketler       = c("Başarısız", "Orta", "Başarılı")
)

# 3) Kantil tabanlı (göreceli) sınıflandırma
set.seed(1)
puanlar <- rnorm(100, mean = 70, sd = 10)
tablo   <- performans_sinifi_ata(
  puanlar,
  yontem  = "kantil",
  n_sinif = 4
)
table(tablo)
}
